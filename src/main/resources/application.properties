# Application name
spring.application.name=notes-backend

# Database Configuration (Dynamic with Environment Variables)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/notes_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:SY28060404}

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
